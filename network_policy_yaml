apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: api-allow
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: C
  podSelector:
    matchLabels:
      app: C
      role: api
